import React, { Component } from 'react';
import {Menu} from "semantic-ui-react";

const Header = () => (
    (
        <Menu>
            <Menu.Menu>
                <Menu.Item>
                    Air Line Logo
                </Menu.Item>
            </Menu.Menu>
        </Menu>
    )
);

export default Header;